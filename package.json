{
  "name": "jood-starter-rollup",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "description": "rollup workspace",
  "license": "MIT",
  "scripts": {
    "bootstrap": "yarn run bootstrap:install && yarn run bootstrap:builds",
    "bootstrap:install": "yarn install --production=false",
    "bootstrap:builds": "yarn workspace @jood-starter-rollup/package-a build && yarn workspace @jood-starter-rollup/package-b build"
  },
  "keywords": []
}
